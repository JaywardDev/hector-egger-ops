// Static page copy for the QA module, kept in one place (mirrors the
// stock-take ui-contract pattern) so wording is easy to review and reuse.

export const QA_EYEBROW = "Quality Assurance";

export const QA_LIST_TITLE = "QA Projects";
export const QA_LIST_DESCRIPTION =
  "Inspection checklists, hold-point sign-off, and evidence — replacing CONQA, in the platform's own workflow.";

// Shown while the module runs on placeholder data, before the C-base import and
// Supabase migration land. Remove once real data is wired in.
export const QA_PREVIEW_NOTICE =
  "Preview: this screen renders sample data to show the shape and flow. Real projects arrive once the C-base import and QA tables are in place.";
